using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurnManager : MonoBehaviour 
    /* im too nooby to know how to do this, but we need this to receive a
     * Javachu.moveOver and Nohtyp.moveOver from each respective class to sense if 
     * the players are done choosing moves and then make each hackemon attack.
     * Then we need to use this class to set each hackemon's moveOver to false, where
     * each hackemon will attack again, and the cycle contnues
     */
{
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
